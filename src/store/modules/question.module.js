const state={

}
const getters={}

const mutations={}

const actions={
    addQuestion(){
        console.log('questin module working')
    }
}

export default{
    state,
    getters,
    mutations,
    actions
}